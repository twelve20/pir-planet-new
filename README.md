# Планета ПИР - Сайт продажи PIR плит

Корпоративный сайт для продажи PIR плит с интеграцией Telegram бота для приема заявок.

## Возможности

- 📱 Адаптивный дизайн
- 🔔 Отправка заявок в Telegram
- 📋 Каталог товаров с подробными характеристиками
- 💰 Двойная система цен (наличные/безналичный расчет)
- 📸 Галерея выполненных работ
- ✉️ Форма обратной связи

## Технологии

- HTML5, CSS3, JavaScript
- Node.js + Express
- Telegram Bot API
- Responsive Design

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd the
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
2. Получите токен бота
3. Узнайте свой Chat ID через [@userinfobot](https://t.me/userinfobot)
4. Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

5. Заполните `.env` файл:

```env
PORT=3000
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
```

## Запуск

### Режим разработки (с auto-reload)

```bash
npm run dev
```

### Продакшн режим

```bash
npm start
```

Сервер запустится на `http://localhost:3000`

## Структура проекта

```
.
├── css/                  # Стили
│   ├── styles.css       # Общие стили
│   ├── catalog.css      # Стили каталога
│   └── ...
├── js/                   # JavaScript файлы
│   ├── script.js        # Основной скрипт
│   └── catalog.js       # Скрипт каталога
├── images/              # Изображения
├── index.html           # Главная страница
├── catalog.html         # Каталог товаров
├── gallery.html         # Галерея
├── contacts.html        # Контакты
├── server.js            # Node.js сервер
├── package.json         # Зависимости проекта
└── .env                 # Конфигурация (не в git)
```

## API Endpoints

### POST `/api/send-order`

Отправка заявки с формы

**Body:**
```json
{
  "name": "Имя клиента",
  "phone": "+7 (999) 123-45-67",
  "comment": "Комментарий (опционально)"
}
```

**Response:**
```json
{
  "success": true,
  "message": "Заявка успешно отправлена",
  "telegramSent": true
}
```

### GET `/api/status`

Проверка статуса сервера

**Response:**
```json
{
  "status": "ok",
  "telegram": true,
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## Деплой

### На VPS/Dedicated сервер

1. Установите Node.js (версия 14+)
2. Скопируйте файлы проекта на сервер
3. Установите зависимости: `npm install`
4. Настройте `.env` файл
5. Используйте PM2 для запуска:

```bash
npm install -g pm2
pm2 start server.js --name planeta-pir
pm2 save
pm2 startup
```

### С использованием Nginx

Пример конфигурации Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

## Безопасность

- ✅ Валидация данных на клиенте и сервере
- ✅ Токен бота хранится в `.env` (не в коде)
- ✅ CORS настроен для безопасности
- ✅ Sanitization пользовательского ввода

## Поддержка

При возникновении проблем:

1. Проверьте файл `.env` (правильность токена и chat_id)
2. Убедитесь, что бот запущен и добавлен в нужный чат
3. Проверьте логи сервера: `npm start` или `pm2 logs`

## Лицензия

Proprietary - Все права защищены
