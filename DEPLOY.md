# Инструкция по деплою в продакшн

## Готовность проекта: ✅

Ваш проект полностью готов к выкладке в сеть!

## Варианты хостинга

### Вариант 1: VPS/VDS (Рекомендуется)

**Подходящие хостинги:**
- [Timeweb](https://timeweb.com) - от 300₽/мес
- [Beget VPS](https://beget.com) - от 250₽/мес
- [Selectel](https://selectel.ru) - от 200₽/мес
- [REG.RU VPS](https://www.reg.ru) - от 350₽/мес

**Установка на VPS:**

1. **Подключитесь к серверу по SSH:**
   ```bash
   ssh root@ваш_ip
   ```

2. **Установите Node.js:**
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```

3. **Установите PM2 (для автозапуска):**
   ```bash
   npm install -g pm2
   ```

4. **Скопируйте проект на сервер:**
   ```bash
   # На локальной машине
   scp -r c:/Users/User/Documents/ProjectsByMe/the root@ваш_ip:/var/www/
   ```

5. **На сервере:**
   ```bash
   cd /var/www/the
   npm install

   # Создайте .env файл с вашими данными
   nano .env
   # Вставьте:
   PORT=3000
   TELEGRAM_BOT_TOKEN=7782157467:AAEiMm_UiBtyYD_5qOHkUQp5uJl60K_poLU
   TELEGRAM_CHAT_ID=-4667528349

   # Запустите через PM2
   pm2 start server.js --name planeta-pir
   pm2 save
   pm2 startup
   ```

6. **Установите Nginx (обратный прокси):**
   ```bash
   sudo apt install nginx

   # Создайте конфигурацию
   sudo nano /etc/nginx/sites-available/planeta-pir
   ```

   Вставьте:
   ```nginx
   server {
       listen 80;
       server_name ваш-домен.ru www.ваш-домен.ru;

       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
           proxy_set_header X-Real-IP $remote_addr;
           proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
       }
   }
   ```

   ```bash
   # Активируйте конфигурацию
   sudo ln -s /etc/nginx/sites-available/planeta-pir /etc/nginx/sites-enabled/
   sudo nginx -t
   sudo systemctl restart nginx
   ```

7. **Установите SSL (HTTPS):**
   ```bash
   sudo apt install certbot python3-certbot-nginx
   sudo certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru
   ```

### Вариант 2: Heroku (Бесплатный вариант устарел)

### Вариант 3: Railway.app (До $5/мес бесплатно)

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Подключите GitHub репозиторий
3. Railway автоматически определит Node.js проект
4. Добавьте переменные окружения:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`
   - `PORT` (Railway установит автоматически)
5. Deploy автоматически запустится

### Вариант 4: Render.com (Бесплатный план доступен)

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - Build Command: `npm install`
   - Start Command: `npm start`
5. Добавьте Environment Variables:
   - `TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_CHAT_ID`

### Вариант 5: Обычный хостинг с Node.js

**Подходящие хостинги:**
- [Beget](https://beget.com) - от 99₽/мес (поддержка Node.js)
- [Timeweb](https://timeweb.com) - от 199₽/мес

## Настройка домена

1. Купите домен (REG.RU, Timeweb, или другой регистратор)
2. Укажите DNS записи:
   - A-запись: `@` → IP вашего сервера
   - A-запись: `www` → IP вашего сервера
3. Подождите 1-24 часа для распространения DNS

## Проверка перед запуском

- [x] Форма отправки заявок работает
- [x] Telegram бот настроен и получает сообщения
- [x] Все страницы открываются
- [x] Все изображения загружаются
- [x] Навигация работает
- [x] Мобильная версия адаптивна
- [x] .env файл не в git репозитории

## Мониторинг

После запуска рекомендуется:

1. **PM2 мониторинг:**
   ```bash
   pm2 monit
   pm2 logs
   ```

2. **Автоматический перезапуск при падении:**
   PM2 делает это автоматически

3. **Обновление проекта:**
   ```bash
   cd /var/www/the
   git pull
   npm install
   pm2 restart planeta-pir
   ```

## Дополнительные рекомендации

### Безопасность:
- ✅ `.env` файл защищен через `.gitignore`
- ✅ Токены бота на сервере
- ✅ Валидация данных формы
- ⚠️ Рекомендуется добавить rate limiting (ограничение частоты запросов)

### Производительность:
- ✅ Изображения в формате WebP
- ⚠️ Можно добавить CDN для статики
- ⚠️ Можно включить gzip сжатие в Nginx

### SEO:
- ⚠️ Рекомендуется добавить:
  - Google Analytics
  - Яндекс.Метрика
  - robots.txt
  - sitemap.xml

## Поддержка

Если возникнут проблемы:

1. Проверьте логи: `pm2 logs planeta-pir`
2. Проверьте статус: `pm2 status`
3. Проверьте Nginx: `sudo nginx -t`
4. Проверьте порты: `netstat -tulpn | grep 3000`

## Быстрый старт для новичков

Если вы не знакомы с серверами, рекомендую:

1. **Railway.app** - самый простой вариант, почти всё автоматически
2. **Timeweb** - русскоязычная поддержка, простая панель управления

Для Railway достаточно:
- Залить проект на GitHub
- Подключить репозиторий к Railway
- Добавить переменные окружения
- Готово! 🚀
